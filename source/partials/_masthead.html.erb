<header class="masthead">
  <div class="group">
    <h1 class="masthead__header"><a href="/">Priory Park Dentalcare</a><% if current_page.data.title %><span class="visuallyhidden"> <%= current_page.data.title %></span><% end %></h1><!--
    --><div class="masthead__contact">
      <p><%= data.contact.phone %></p>
      <p>
        <% data.contact.address.each do |text| %>
          <span><%= text %></span>
        <% end %>
      </p>
    </div>
  </div>
  <nav class="nav">
    <ul class="nav__list">
      <!--<% data.pages.masthead.each do |nav| %>
        --><li class="nav__item">
          <% label = nav.abbr ? "<abbr class='nav__label' title='#{nav.abbr.title}'>#{nav.abbr.label}</abbr>" : "<span class='nav__label'>#{nav.label}</span>" %>
          <a title="<%= nav.alt %>" class="nav__link--<%= nav.icon || nav.slug %> ss-nav-<%= nav.icon || nav.slug %>" href="/<%= nav.slug %>"><%= label %></a>
        </li><!--
      <% end %>-->
    </ul>
  </nav>
</header>